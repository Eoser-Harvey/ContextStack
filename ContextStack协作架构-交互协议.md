# ContextStack 协作架构 - 交互协议

## 协议概述

本文档定义 ContextStack 四层架构中各层之间的交互协议，确保AI助手在不同场景下正确加载和使用规则、文档和记忆。

## 层间交互流程

### 启动流程
```
AI助手启动
    → 加载第一层: GLOBAL-RULES.md (常驻内存)
    → 检查当前工作目录
    → 加载第二层: PROJECT-RULES.md (如果存在)
    → 读取第四层: MEMORY.md (了解上下文)
    → 等待用户指令
```

### 项目切换流程
```
用户发出切换指令
    → 保存当前工作台状态
    → 清理不相关Context
    → 加载新项目的 PROJECT-RULES.md (第二层)
    → 打开对应工作台文件
    → 恢复相关Memory上下文 (第四层)
    → 确认切换完成
```

### 任务执行流程
```
用户发出任务指令
    → 分析任务类型和需求
    → 按需加载规范文档 (第三层)
    → 执行任务 (使用工具、编辑文件)
    → 更新工作台状态
    → 必要时更新Memory索引 (第四层)
    → 报告结果
```

### 会话结束流程
```
用户发出结束指令
    → 保存当前工作台状态
    → 更新Memory索引 (第四层)
    → 记录本次会话摘要
    → 确认保存完成
```

## 层级优先级规则

### 规则冲突处理
1. **全局规则 (第一层)** 优先级最高，不可被下层覆盖
2. **项目规则 (第二层)** 可细化全局规则，但不能违反
3. **规范文档 (第三层)** 提供参考，不具强制性
4. **Memory (第四层)** 记录事实，不定义规则

### Context加载优先级
1. 安全红线 (第一层) - 必须最先检查
2. 项目约束 (第二层) - 任务执行前检查
3. 方法论指导 (第三层) - 需要时加载
4. 历史记忆 (第四层) - 提供上下文

## 工作台交互协议

### 工作台文件格式
```markdown
# {项目/话题名称} 工作台

## 基本信息
- 创建时间
- 最后更新
- 当前状态

## 当前任务
- 任务描述
- 进度
- 阻塞项

## 会话历史
- 日期: 内容摘要

## 待办事项
- [ ] 事项1
- [ ] 事项2

## 决策记录
- 日期: 决策内容和理由
```

### 工作台状态转换
```
空闲 → 活跃 (开始工作)
活跃 → 暂存 (切换话题)
暂存 → 活跃 (恢复工作)
活跃 → 完成 (项目结束)
```

## Memory更新协议

### 更新触发条件
- 项目状态变更
- 收到重要反馈
- 发现关键资源
- 完成重要里程碑
- 用户明确要求

### 更新内容规范
- 记录时间戳
- 描述变更内容
- 标注重要程度
- 关联相关项目

## 文件操作协议

### 创建文件
1. 检查是否已存在
2. 选择合适的模板
3. 创建文件并填充内容
4. 更新相关索引文件

### 编辑文件
1. 读取并理解原有内容
2. 执行编辑操作
3. 必要时创建备份
4. 更新相关文档

### 删除文件
1. 确认删除必要性
2. 检查文件关联
3. 执行删除
4. 更新索引文件

## 错误处理协议

### 规则加载失败
- 第一层加载失败: 停止工作，报告错误
- 第二层加载失败: 使用全局规则继续，提示用户
- 第三层加载失败: 跳过，使用通用方法
- 第四层加载失败: 创建新的Memory索引

### 工作台恢复失败
- 尝试从备份恢复
- 如无法恢复，创建新工作台
- 保留损坏文件供用户检查

---

**版本**: v1.0
**创建时间**: 2026-04-30
**最后更新**: 2026-05-07
