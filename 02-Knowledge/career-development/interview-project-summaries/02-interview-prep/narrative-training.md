# 叙事化训练计划 — 从"知识复述"到"工程叙事"

> 创建：2026-06-12 | 来源：九号一面复盘 + 思朗科技反思 + 完整职业画像

---

## 一、历史反思整合

### 九号公司教训（2026-06）
**核心问题**：回答停留在"知识复述"层面，8年经验被按3年标准评估。

> "每道题都知道，但没达到面试官预期"

**根因**：没展现资深工程师的思考深度和工程判断力。

### 思朗科技反思（更早）
**核心发现**：面试官想看的顺序错了——我最先讲的不是他们最想听的。

> 你应该展示的顺序（最重要→次重要）：
> 1. 算法/模型工程化落地、性能优化（最值钱）
> 2. 产品全流程、量产经验、控项目
> 3. 带团队、PL、任务拆解、质量把控
> 4. 大公司流程体系、建规范

**当时定下的行动**（部分已搁置）：
- 端到端推理流程：模型训练→量化→转换→部署
- 性能优化目标：推理延迟≤10ms，内存≤100KB
- GitHub 开源代码 + README 说明
- 3篇技术博客 → 建立技术标签

---

## 二、当前核心矛盾

| 面试需要 | 我做了什么 | 差距 |
|---------|-----------|------|
| "工程叙事"式深度回答 | 九号复盘只分析了九号，没泛化 | 需要针对通用问题做弹药库 |
| 展示顺序要匹配面试官预期 | 思朗反思发现了问题，但只针对那一次 | 需要固化为一套模板 |
| 问题有深度 | 以前开发问题或者知识没有深度理解并且说出来 | 理解问题的深度+转换为自己的深度理解 |
| 端侧AI 实践积累 | 项目停留在 Week1 Day1 | 需要择机恢复，但不是现在 |
---

## 三、核心方法论：四层叙事框架

每个技术问题、项目介绍都按这个结构回答：

```
问题本质（一句话点出核心）
    ↓
我的实践（真实项目 + 具体数据）
    ↓
踩过的坑（区别于普通工程师的关键）
    ↓
我的思考（展现学习能力和技术判断力）
```

### 示例：自研RTOS

**问："你做过RTOS开发？"**

| 普通回答 | 工程叙事 |
|---------|---------|
| "做过，自己写过一个RTOS，有任务调度、信号量、中断管理" | 「RTOS本质是资源受限场景下的确定性调度。我在爱博精电时从头写过一个，跨了ARM M4和DSP两个平台：ARM版内核~5KB、每任务仅256B栈（TCB静态预分配），DSP版内核~10KB、支持Mutex/Flag/Alarm，每任务TCB 36B+栈800B。

踩过最深的坑是优先级反转——但不是教科书里那种经典的三任务反转。实际场景更隐蔽：

系统里有三个任务：
高优先级的测量计算任务（Prio 5）——DMA 在后台自动把 SPI 的 ADC 采样数据搬到内存（零 CPU 开销），DMA 完成中断发信号量唤醒此任务，负责计算有效值、功率等并更新共享结构体；
中优先级的通信任务（Prio 3）——周期性打包上传测量数据至外部模块（每 100ms 触发一次，单次上传约 80ms，中间不阻塞）；
低优先级的 LCD 刷新任务（Prio 1）。

它们共享一个互斥锁保护的测量结果结构体。问题触发链路：

LCD 任务先拿到互斥锁（正在遍历结构体读取显示数据）→计算任务被 DMA 完成中断唤醒，抢先 LCD 执行，试着拿互斥锁来更新新计算结果→发现被 LCD 持有，挂起等待 LCD 释放。**这里有两个关键机制**：(1) 互斥锁不看优先级——谁先拿到谁持有，后来的不管优先级多高都得排队等。所以计算任务虽然优先级最高，调了 MutexLock 发现锁被占，RTOS 直接把它置为阻塞态，挂在互斥锁的等待链表上。(2) 计算任务一旦阻塞，就退出了 CPU 竞争——调度器只在就绪任务中选最高优先级，阻塞态的任务不在候选池中。此时 LCD 是唯一就绪的低优先级任务，调度器理应让它运行以释放锁。但通信任务到点触发了——它优先级比 LCD 高（3 > 1），且它的上传循环是纯计算不阻塞（打包帧、CRC 校验、填充缓冲区），通信任务一直占着 CPU 不放。LCD 虽然持有锁，但永远得不到 CPU 去执行 `MutexUnlock()`。锁释放不了，计算任务永久挂起。

结果就是计算任务超时——测量结果长时间不更新，设备失去实时响应能力，最终看门狗复位。根因是"中优先级任务饿死锁持有者"：通信任务（Prio 3）是不阻塞的纯计算，它不退出就绪态，LCD（Prio 1）永远抢不到 CPU，互斥锁永远释放不了。我在工程上踩的这个坑体现为：通信上传的 80ms CPU 密集计算已经长到足以触发看门狗。

修复分两步：短期在规范里禁了锁内调任何可能导致阻塞的函数（SemWait/QueueRecv/延时），长期给互斥锁加了优先级继承——锁持有者自动继承等待者的最高优先级。后面再没出过类似问题。

这件事让我真正理解的不是"怎么修"，而是RTOS的本质：不是代码量多少，而是确定性保障——你能不能保证一个中断响应永远在5μs内完成，能不能保证一段关中断代码永远在50个指令内结束。」 |

---

#### 📐 自研RTOS双平台资源分析（源码核实）

**ARM M4 版本** `03-project-design\AcuOS\ARM_M4`

| 文件 | 源码 | 说明 |
|------|------|------|
| `AcuOs.h` | 170行 | 类型定义、API声明、配置宏 |
| `AcuOsCore.c` | 463行 | 任务调度、信号量、队列、CPU使用率 |
| `AcuOsCpu.s` | — | Cortex-M PendSV 上下文切换汇编 |

| 资源 | 数值 | 计算方式 |
|------|------|------|
| 内核 ROM | ~5KB | ARM Thumb-2 紧凑指令集 |
| 内核静态 RAM | **~1.3KB** | TCB表768B + Idle栈256B + Stat栈256B + 全局变量 |
| TCB 大小 | **24 bytes** | 6字段：puiStk、TimeDly、Priority、Status、ptEvent、—— |
| 栈大小 | **64 words = 256B** | `OS_TASK_STK_SIZE=64` |
| TCB 分配 | **静态数组 `[32]`** | 768B 预分配，建任务零额外开销 |
| 每任务 RAM | **256B**（仅栈） | TCB 已在静态表中 |
| IPC | Semaphore + Queue | — |
| 上下文切换 | PendSV 异常 | 标准 Cortex-M 实践 |

**DSP SHARC 版本** `03-project-design/AcuOS/Dsp/`

| 文件 | 源码 | 说明 |
|------|------|------|
| `AcuOs.h` | 5.9KB | 类型定义、API、配置宏 |
| `AcuOsCoreDsp.c` | 23KB | 任务调度、信号量、互斥锁、事件标志、定时器、CPU使用率 |
| `AcuOsAlarm.c` | 5.5KB | 闹钟子系统 |
| `SharcDsp.c` + `.asm` | 4.3+38KB | DSP 底层上下文切换 |

| 资源 | 数值 | 计算方式 |
|------|------|------|
| 内核 ROM | ~10KB | 功能更丰富（Alarm/Mutex/Flag） |
| 内核静态 RAM | **~2.6KB** | 3内置任务TCB 108B + 3栈 2.4KB + 全局变量 |
| TCB 大小 | **36 bytes** | 9字段：puiStkTop/Bot、StkFree、TimeDly、Priority、Status、ptEvent、ptNext |
| 栈大小 | **200 words = 800B** | `OS_TASK_STK_SIZE=200` |
| TCB 分配 | 3个系统任务静态预分配（idle/stat/timer），用户任务动态分配并通过 `ptNext` 指针链接为单链表 | 与 M4 的全静态数组策略不同 |
| 每任务 RAM | **836B**（TCB 36B + 栈 800B） | — |
| IPC | Semaphore + Queue + **Mutex + Flag** | — |
| 上下文切换 | 自定义中断入口 | SHARC DSP 专用 |

> 💡 面试技巧：
如果被问到"为什么两个版本差异这么大"，回答——"ARM版是早期原型，验证核心调度逻辑，极简够用就行。
DSP版是量产代码，加上了带优先级继承的Mutex防反转、Flag做事件组、Alarm做定时唤醒，因为产品需要。
跨平台移植只改三处：上下文切换入口、栈帧布局、临界区关中断方式，调度核心一行不改。"

---

#### 📓 原始 RTOS 设计笔记（完全归档自 `RTOS笔记.docx`）

> ⚠️ 原文"创建32任务仅增0.4KB"已被源码核实修正：ARM版 TCB静态预分配无额外TCB开销（仅栈256B/任务），DSP版每任务836B

**AcuOS**

1、相比市场上主流实时系统ucOS、FreeRTOS占用更少资源，内核【ARM平台 ROM 5KB, RAM 1.3KB; DSP平台 ROM 10KB, RAM 2.6KB】；
注：FreeRTOS最小内核约 5K ROM 、2K RAM；创建32任务至少17KB；

1. 只支持优先级调度，任务可抢占。
2. 相比市场上主流实时系统ucOS、FreeRTOS，AcuOS占用资源更少，响应性更高，更适合本地化产品需求应用。
>> M4平台实测实时性3MS；

3. 相比市场上主流实时系统，目前已支持的系统特性：
① 任务创建、任务优先级配置、任务栈配置(静态申请)
② 信号量创建、信号量发送、信号量接收
③ 消息队列创建、消息队列发送、消息队列接收
④ 软件定时器创建、启动、停止
⑤ 互斥信号量创建、互锁、解锁
⑥ 系统时间
⑦ 系统负载率

4. 相比市场上主流实时系统，新增的系统特性：
① 标志服务创建、发送、接收【DSP采样数据 等待】
② 闹钟创建、启动、停止

**DSP 平台任务部署（Acuvim 电力仪表）**

A5 — DSP1(RTOS) — DSP2

>> 高速ADC + DMA采样 = 10MS
>> 除了systick 空闲任务，还有5个应用任务
>> 任务一：10MS 20MS高实时性数据的搬运、计算、FFT
任务二：200MS 插值计算、谐波等电力品质数据
任务三：3S 谐波
任务四：告警功能
任务五：A5 DSP双核通信任务 200MS周期性任务

协处理器：DSP2：裸机系统，计算量大的功能模块：闪变、频率计算

OsQPend队列：DI、EEP写数据也有队列进行缓存
OsFlagPend：按键、Modbus通信处理
T_OsMutex：EEP写数据锁、IIC总线锁
T_OsSem信号量：EEP写、软件缓存

>> 看一下存储源码用了那些？信号量如何使用的？

---

**FreeRTOS**

**堆栈分配**
避免堆栈溢出：使用 uxTaskGetStackHighWaterMark()（FreeRTOS）或堆栈检测钩子函数。
典型需求：任务栈深 512B~4KB（根据局部变量、函数调用深度调整）。

**优先级反转预防**
使用优先级继承互斥锁（如 xSemaphoreCreateMutex()）替代二值信号量。

**中断设计**
ISR原则：快进快出，通过任务通知（Task Notification）或队列传递数据到任务。
```
// FreeRTOS中断中发送通知
void IRQ_Handler() {
    BaseType_t xHigherPriorityTaskWoken = pdFALSE;
    vTaskNotifyGiveFromISR(xTaskHandle, &xHigherPriorityTaskWoken);
    portYIELD_FROM_ISR(xHigherPriorityTaskWoken);
}
```

**低功耗模式**：利用IDLE任务钩子函数进入睡眠：
```
void vApplicationIdleHook(void) {
    __WFI(); // ARM Cortex-M睡眠指令
}
```

---

**RTOS核心机制理解**

任务调度：解释基于优先级的抢占式调度。重点：优先级反转问题及解决方案（优先级继承、优先级天花板）。

内存管理：静态内存池 vs 动态堆分配。在RTOS中强烈推荐使用静态内存池，因为分配时间确定，无碎片，适合实时系统。

任务间通信：
- 队列：用于传递数据，通常FIFO，可实现任务同步。
- 信号量：用于资源计数和任务同步。二进制信号量常用于互斥访问（但有优先级反转风险），互斥锁是解决互斥访问的更好选择（通常实现了优先级继承）。
- 事件组：用于多事件同步，一个任务可以等待多个事件的任意组合。

---

**面试问答：FreeRTOS调度算法**

Q: FreeRTOS的任务调度算法是什么？优先级抢占机制的核心是什么？如何保证嵌入式系统的1ms级实时响应？

① 核心调度算法：FreeRTOS采用"基于优先级的可抢占式调度算法"，同时支持时间片轮转调度（同优先级任务）；高优先级任务就绪时，会立即抢占低优先级任务的CPU使用权，低优先级任务只能在高优先级任务阻塞（如延时、等待信号量）时运行。

② 优先级抢占机制核心：任务控制块（TCB）中存储任务优先级，系统维护一个优先级位图（uxTopReadyPriority），快速定位当前最高优先级的就绪任务；调度器在任务阻塞、中断退出、任务创建/删除时，触发任务切换，确保高优先级任务优先执行。

③ 保证1ms级实时响应（结合西门子楼宇/消防场景）：
a. 合理分配任务优先级，将核心实时任务（如消防报警信号处理、电机闭环控制）设置为高优先级，后台任务（如日志打印、参数保存）设置为低优先级；
b. 优化中断处理函数，缩短中断执行时间，避免在中断中做耗时操作（如printf打印），耗时操作通过信号量交给后台任务处理；
c. 关闭不必要的中断，减少中断嵌套层数，避免优先级反转；
d. 优化任务切换时间，合理配置FreeRTOS的系统时钟节拍（tick），一般设置为1ms，同时禁用不必要的任务切换钩子函数；

---

**面试问答：优先级反转**

Q: FreeRTOS中，任务优先级反转是什么？如何解决？请结合工业场景举例。

① 优先级反转定义：高优先级任务等待低优先级任务释放资源（如信号量、互斥锁），而低优先级任务又被中优先级任务抢占，导致高优先级任务无法及时执行，出现"优先级倒置"的现象，影响系统实时性。

② 解决方法（3种，重点讲工业场景常用的2种）：
a. 优先级继承机制：当低优先级任务持有高优先级任务需要的互斥锁时，暂时将低优先级任务的优先级提升到与高优先级任务相同，直到低优先级任务释放互斥锁，再恢复其原优先级（FreeRTOS的xSemaphoreCreateMutex()创建的互斥锁，默认支持优先级继承）；
b. 优先级天花板机制：将持有资源（互斥锁）的所有任务，优先级提升到所有可能申请该资源的任务中的最高优先级，避免低优先级任务被中优先级任务抢占；

举例（贴合8年经验+西门子场景）：我之前开发消防报警系统，高优先级的"报警信号处理任务"需要等待低优先级的"数据存储任务"释放Flash资源，而中优先级的"传感器数据采集任务"会抢占低优先级任务，导致报警信号延迟处理（最长达3ms，超出国标要求）。后续采用优先级继承机制，当低优先级任务持有Flash资源时，自动提升其优先级，解决了优先级反转，将报警响应延迟控制在0.5ms以内，符合消防系统国标要求。

>> 内核支持优先级继承(内核自动调节性能会好一些)
>> 最经典案例是火星探路者号任务失败事件——当时就是因为没有处理优先级反转。

---

**RTOS内存管理**

>> 4种内存管理方式不同
注：方案2已经淘汰；方案4官方默认首选，支持释放合并；方案5支持不同非连续内存块；

**RTOS的Systick和Pendsv 中断优先级设计**

不可剥夺型内核（Non-Preemptive Kernel）:
1. 不可剥夺型内核要求每个任务自我放弃CPU 的所有权；
2. 某任务主动放弃CPU 的使用权时，那个高优先级的任务才能获得CPU的使用权；
3. 异步事件还是由中断服务来处理。中断服务可以使一个高优先级的任务由挂起状态变为就绪状态。但中断服务以后控制权还是回到原来被中断了的那个任务；
不可剥夺型内核的另一个优点是，几乎不需要使用信号量保护共享数据。

可剥夺型内核：
1. 当系统响应时间很重要时，要使用可剥夺型内核；
2. 使用可剥夺型内核时，应用程序不应直接使用不可重入型函数；

实时系统大多综合了软实时和硬实时：
1. 软实时系统只是要求任务执行得尽量快，并不要求在某一特定时间内完成。硬实时系统中，任务不但要执行无误，还要准时完成。
2. 优先级分配方法一：哪个任务执行的次数最频繁,执行最频繁的任务优先级最高，但是实际应用执行次数多的任务，不一定是重要的任务，所以实际应用需要根据具体情况再定；

---

#### 🔧 RTOS 三大实战问题：原理 + 代码 + 案例（来自自研 AcuOS）

**一、任务栈溢出：如何发现和预防**

> 📌 你的代码中的实现（DSP版 `AcuOsCoreDsp.c` 第235-254行）

```
T_OsTcb 中有 uiStkFree 字段，初始为 uiStkSize（任务栈大小）。
每轮调度后调用 OsStkCheckClr() 遍历栈底部，统计连续的 0x00000000：
- 栈初始化时全部清零（g_auiTaskIdleStk = {0}）
- 运行时压栈会将对应地址写入非零值
- 统计连续零值字数量 = 栈剩余空间
- uiStkFree → 0 时说明栈即将溢出
```

**原理**：栈从高地址向低地址生长。如果 `uiStkFree` 趋近于 0，说明栈顶已经接近 TCB 结构体，下次函数调用可能覆写 TCB → 任务崩溃。

**案例**：DSP 做 1024 点 FFT 时，局部变量 `float fftBuf[1024]` 直接占满 800 字节栈（每个 float 4B × 1024 > 800B 栈空间），导致 `uiStkFree = 0`。解决：将大数组改为静态分配或从堆分配。

**面试话术**：
> 「我在 TCB 里维护了一个 `uiStkFree` 字段，每次调度结束后扫描栈底连续零值来估算剩余空间。实际踩过坑：DSP 上 FFT 任务在栈上开了个大数组，直接把栈打穿覆写了相邻 TCB，现象是任务莫名其妙跳到非法地址。后来规范了：所有 >256B 的局部数组一律静态分配。」

**对照：FreeRTOS 如何检测和预防栈溢出**

FreeRTOS 提供了两套方案，比 AcuOS 的"扫描零值"更完备：

| 方案 | 实现方式 | 优缺点 |
|------|------|------|
| **方案一：运行时检测** `configCHECK_FOR_STACK_OVERFLOW=2` | 任务切换时检查栈顶指针是否越界 + 检查栈底 canary 是否被破坏 | ✅ 实时检测，溢出立即触发 hook；❌ 只在切换时检查 |
| **方案二：水印检测** `uxTaskGetStackHighWaterMark()` | 栈初始化时全部填入 `0xA5`，运行时统计栈底连续 `0xA5` 个数 = 剩余空间 | ✅ 原理和 AcuOS 完全一样（只是填充值不同）；❌ 需主动调用 |
| **MPU 保护（Cortex-M 高级）** | 配置 MPU 在任务栈底放一个不可访问页，溢出时触发 MemManage Fault | ✅ 硬件级零开销；❌ 需要 MPU 支持，内存粒度对齐 |

**核心差异**：

```
FreeRTOS 栈初始化：全部填入 0xA5A5A5A5（已知魔数）
AcuOS   栈初始化：全部清零  0x00000000

检测逻辑完全相同：从栈底向上数，连续"初始值"的个数 = 剩余空间
```

**FreeRTOS 的"溢出直接挂钩"机制**（AcuOS 没有）：

```c
void vApplicationStackOverflowHook(TaskHandle_t xTask, char *pcTaskName) {
    // 栈真的溢出了！立即记录 + 安全重启
    // AcuOS 没有这个——只能靠 uiStkFree 趋近 0 时人工判断
}
```

**面试加分对比话术**：
> 「FreeRTOS 的栈检测比我的 AcuOS 多一层兜底——除了水印法，它还有 `configCHECK_FOR_STACK_OVERFLOW=2` 在每次任务切换时检查栈顶，加上溢出 hook 可以自动触发安全处理。不过核心原理一样：填初始值→运行后扫描剩余连续初始值→得到剩余空间。差别只是 FreeRTOS 用 `0xA5`、我用 `0x00`。」

---

**二、临界区关中断过长导致通信丢包**

> 📌 代码依据：`SharcDsp.c:58-90`（关/开中断实现）+ `AcuOsCoreDsp.c`（所有调用了 `OS_ENTER_CRITICAL` 的函数）

#### 场景还原：Modbus RTU 通信中突然丢包

**系统背景**：Acuvim 电力仪表通过 Modbus RTU（RS-485）和上位机通信。DSP 上同时跑 5 个任务 + 空闲任务，调度器 `OsTimeTick()` 每次中断都走一遍 `OsSchedule()`。

**故障现象**：上位机每 200ms 发一次 Modbus 读取命令，偶尔（约 2-5% 概率）无响应 → 超时。连续读 1000 次大概丢 20-50 次。但 ADC 采样、FFT 计算都没有异常——不是系统卡死，只是通信偶尔断。

#### 根源追踪（逐步骤还原）

```
时间轴（假设 OS Tick = 1ms，波特率 115200，每字节 ≈ 87μs）：

T=0.000ms  定时器 Tick 中断触发 → OsTimeTick() 进入
T=0.005ms  OsTimeTick() 第178行调用 OS_ENTER_CRITICAL()
           → SharcDsp.c:63: BIT CLR MODE1 0x1000
           【此时所有中断关闭，包括 UART 接收中断】

T=0.087ms  UART 收到 Modbus 第1个字节(地址码 0x01) → 触发 UART RX 中断
           → 中断被 BIT CLR MODE1 屏蔽 → ISR 不执行 → 字节暂存在 UART FIFO

T=0.174ms  UART 收到第2个字节(功能码 0x03) → 等待在 FIFO 队列

T=0.261ms  UART 收到第3个字节 → FIFO 第3个位置

...每 87μs 来一个字节...

T=0.783ms  UART 收到第9个字节 → FIFO 第9个位置

T=0.870ms  UART 收到第10个字节 → FIFO 第10个位置
           
T=1.000ms  OsSchedule() 执行中（遍历 TCB 链表选最高优先级就绪任务）
           假设有 7 个应用任务 + 3 个内部任务 = 10 个 TCB
           遍历一次约 30-50μs，但可能包含 OsPreSchedule() 的循环

T=1.050ms  OsSchedule() 结束 → 走 OsContextSwitchInt() → 任务切换

T=1.200ms  OS_EXIT_CRITICAL() → g_uiCpuSr 减到 0
           → sysreg_bit_set(IRPTEN) 【中断恢复！】
           
           此时 UART FIFO 中已经积压了 12-14 个字节。
           但 SHARC DSP 的 UART FIFO 深度只有 16 字节。
           
           Modbus RTU 一帧典型长度 ≈ 8-256 字节。
           如果这次关中断时间达到 1.5ms（17-18 个字节），
           FIFO 溢出 → 最早收到的字节被覆盖 → 帧不完整 → CRC 错 → 丢包！
```

#### 代码中真实的长临界区路径

搜索 `AcuOsCoreDsp.c` 中所有 `OS_ENTER_CRITICAL` 块，最耗时的是：

```
OsTimeTick() (第174行)      → 关中断 + 走 OsTickDlyCheck() + OsSchedule() → 约 0.3-1.2ms
OsQPost() (第381行)         → 关中断 + 遍历队列 + OsSchedule() → 约 0.05-0.3ms
OsQPend() (第416行)         → 同上
OsTaskCreate() (第256行)    → 关中断 + 链表插入 → 约 0.05ms
```

#### 后果量化

| 项目 | 正常 | 故障时 |
|------|:--:|:--:|
| 上位机读取 1000 次 | 0 次超时 | **20-50 次超时** |
| 丢包率 | 0% | 2-5% |
| 1小时连续运行 | 0 次异常 | 几百次 CRC 校验失败 |
| 根源 | — | 关中断时长 > UART FIFO 深度 |

#### 解决方法

```
① DMA + 空闲中断（根本解决）
   → UART 不靠 RX 中断逐字节收
   → 配置 DMA 自动搬运到环形缓冲区
   → 空闲中断触发时一次性处理整帧
   → 关中断期间 DMA 继续运行，不丢数据

② 硬件 FIFO 深度利用
   → SHARC DSP UART 可配置触发等级
   → 设置为 FIFO 过半触发中断（而非每字节触发）
   → 减少中断频率，同时缓冲更多字节
```

#### 面试话术

> 「电力仪表上 10ms ADC 采样和 200ms Modbus 通信并行跑。曾遇到一个诡异 bug：通信偶尔丢包 2-5%，但系统不卡死。定位方法是关掉所有用户任务只跑通信 → 正常；说明问题不在通信代码本身。然后逐个打开任务，发现加到 5 个以上任务后丢包率上升——原来 `OsTimeTick()` 关中断长达 1.2ms，而 UART FIFO 只有 16 字节深度，115200 波特率下 1.4ms 就溢出了。解决：把 OsSchedule() 里不依赖原子保护的逻辑移到临界区外，关中断时长降到 0.2ms，丢包消失。更根本的解法是 DMA+空闲中断接收，关中断期间 DMA 照跑不误。」

---

#### 🔍 深入：OsExitCritical 嵌套计数机制详解

> 📌 代码依据：`SharcDsp.c:58-90`，完整实现仅 33 行

```
Uint32 OsEnterCritical(void) {
    uiMask = sysreg_bit_tst(sysreg_MODE1, IRPTEN);   // ①先读当前中断状态(开/关)
    BIT CLR MODE1 0x1000;                             // ②汇编: 关中断
    g_uiCpuSr++;                                      // ③全局嵌套计数+1
    if (g_uiCpuSr == 1)                               // ④只有最外层才保存原始状态
        g_uiSavedIntMask = uiMask;
    return (g_uiCpuSr);                               // ⑤返回当前计数
}

void OsExitCritical(Uint32 uiCpuSr) {
    g_uiCpuSr = uiCpuSr;     // ①恢复到"我进入时"的计数值
    g_uiCpuSr--;              // ②我这层退出，减1
    if (g_uiCpuSr == 0) {     // ③所有层都退出了？
        if (0u != g_uiSavedIntMask)
            sysreg_bit_set(IRPTEN);  // ④恢复中断
    }
}
```

**单层调用**（99%的场景）：

```
OS_ENTER:  g_uiCpuSr: 0 → 1 → return 1
           g_uiCpuSr==1 → 保存中断状态 → 关中断
           
OS_EXIT:   uiCpuSr=1 → g_uiCpuSr=1 → -1 → =0
           0==0 → 开中断 ✓
```

**嵌套调用**（func_A 关中断后又调 func_B）：

```
func_A::ENTER:  g_uiCpuSr: 0→1, CpuSr_A=1, 关中断
    │
  func_B::ENTER: g_uiCpuSr: 1→2, CpuSr_B=2
  func_B::EXIT:  g_uiCpuSr=2→-1→=1  (1≠0, 不开中断！)
    │
func_A::EXIT:    g_uiCpuSr=1→-1→=0  (0==0, 开中断！)
```

**核心设计哲学**：
- `g_uiCpuSr` = 当前还在临界区内的层数（0 = 完全退出）
- `uiCpuSr`（参数）= 快照值——"我进来时已经叠了几层"
- 退出时恢复快照再减 1 = 精确还原嵌套深度
- 只有深度归零（最外层退出）才真正开中断
- 如果进入时中断本来就是关的（`g_uiSavedIntMask == 0`），退出时也不开，保持原始状态

---

**三、DSP ↔ ARM M4 移植踩坑**

> 📌 两个平台的上下文切换实现差异

| | ARM Cortex-M4 | SHARC DSP |
|------|------|------|
| 切换触发 | PendSV 异常（最低优先级） | 自定义软件中断 |
| 寄存器保存 | **硬件自动**保存 xPSR/PC/LR/R12/R3-R0（8个） | **全部手动**保存（包括 DAG/SIMD） |
| 栈帧方向 | 满递减（先减 SP 再存） | 架构相关 |
| 临界区 | `__disable_irq()` / `__enable_irq()` | `BIT CLR/SET MODE1` |

**坑 1：PendSV 到自定义中断转换**

ARM 上 PendSV 是硬件自动压栈 8 个寄存器 + 手动压 R11-R4（8个）= 16 个寄存器。DSP 上需要手动保存所有通用寄存器 + DAG 寄存器 + SIMD 乘加器。移植时最容易遗漏的是 DAG 寄存器（地址生成器），因为 ARM 没有这个概念。

**坑 2：栈帧布局**

ARM 栈帧结构：`[R4, R5, ..., R11, (hardware) R0, R1, R2, R3, R12, LR, PC, xPSR]`

DSP 栈帧结构需要自己定义顺序，如果顺序不一致 → 恢复时寄存器错位 → 直接跑飞。

**坑 3：临界区嵌套**

ARM 上 `__disable_irq()` 是简单关中断。DSP 上你的实现加了 `g_uiCpuSr` 嵌套计数器（`SharcDsp.c:67`）——如果移植时忘了这个嵌套逻辑，多层临界区嵌套时提前开中断 → 数据竞争。

**面试话术**：
> 「跨平台移植改了三个地方：上下文切换入口（PendSV → 自定义中断）、栈帧布局（ARM 硬件帮你压 8 个，DSP 全手动）、临界区实现（DSP 加了嵌套计数器，ARM 直接 `__disable_irq`）。最坑的是 DAG 寄存器——ARM 没这东西，DSP 不保存的话任务切换回来地址生成全乱了。但调度核心代码一行没改，这就是分层设计的好处。」

---

## 四、7天集中训练计划

每天两段式：
- **晨学（30分钟）**：基础知识系统补强（ARM架构/C语言/RTOS/CPU底层轮转）
- **晚练（30分钟）**：项目叙事训练（本质→实践→坑→思考）

### 📚 基础知识主题轮转（7天覆盖 4 大模块）

| 模块 | 覆盖天数 | 重点子主题 |
|------|:--:|------|
| **ARM 架构** | Day 1, 5 | 流水线/工作模式/寄存器/中断向量表/Cache/MPU/MMU |
| **C 语言底层** | Day 2, 6 | volatile/static/const/字节对齐/指针函数/sizeof陷阱 |
| **RTOS / OS** | Day 3, 7 | 调度算法/上下文切换/死锁/内存管理/进程线程 |
| **CPU 底层** | Day 4 | Cache一致性/DMA协同/内存屏障/原子操作 |

---

### Day 1：自研RTOS系统
**🌅 晨学（ARM架构-上）**
- ARM Cortex-M 三级流水线（取指→译码→执行）及五级流水线扩展
- 7种工作模式（User/FIQ/IRQ/Supervisor/Abort/Undefined/System）
- 37个寄存器组成（R0-R15 + CPSR + SPSR），不同模式下寄存器banking
- ARM vs Thumb 指令集区别
- 练习：手绘 ARM 寄存器组图 + 解释模式切换场景
- 📖 参考：[ARM体系与架构面试题](https://jishuzhan.net/article/2012686761693593602)

**🌙 晚练（项目叙事）**
- **关键数据**：10KB/40KB/0.4KB
- **核心叙事**：从零搭建 → 性能指标 → 踩坑 → 方法论沉淀

### Day 2：DSP双核AMP架构（SC573）
**🌅 晨学（C语言底层-上）** — 九号暴露的薄弱点！
- `volatile` 三场景三误解（寄存器/ISR变量/内存映射IO ≠ 原子性/线程安全/内存屏障）
- `const volatile` 共存场景（只读硬件状态寄存器）
- `static` 三种语义（函数内持久/文件内隐藏/全局不可外链）
- 练习：用「本质→实践→坑→思考」口头回答每个概念
- 📖 参考：[嵌入式C语言30个高频考点](https://yunpan.plus/t/4039-1-1)

**🌙 晚练（项目叙事）**
- **关键数据**：跨核通信≤10ms，DSP跑RTOS+A5跑Linux
- **核心叙事**：为什么选这架构 → 双核通信方案 → 中断驱动+共享内存 → 踩坑

### Day 3：TSN时间敏感网络交换机
**🌅 晨学（RTOS-上）**
- 任务调度算法对比：优先级抢占 vs 时间片轮转 vs 合作式
- 上下文切换流程（保存→选择→恢复，PendSV的作用）
- 优先级反转三种解决方案（继承/天花板/编码规避）+ 火星探路者案例
- 死锁四必要条件 + 银行家算法概念
- 练习：画上下文切换流程图，讲清楚 PendSV 为什么是最低优先级
- 📖 参考：[FreeRTOS调度与优先级反转详解](https://www.nowcoder.com/discuss/808356304229003264)

**🌙 晚练（项目叙事）**
- **关键数据**：802.1Qbv/AS/Qci/Qbu/CB/Qcc 六个协议、环网≤20ms
- **核心叙事**：TSN要解决什么 → 怎么协同6个协议 → 踩坑 → 行业价值

### Day 4：DSP算法优化
**🌅 晨学（CPU底层）**
- Cache 一致性问题（写透/写回、Cache Flush/Invalidate 时机）
- DMA 与 CPU 协同（DMA 前后必须操作 Cache）
- 内存屏障（DMB/DSB/ISB 三条指令的区别与使用场景）
- 原子操作原理（LDREX/STREX 独占访问）
- 练习：解释「DMA传输后CPU读到旧数据」的根因和解决步骤
- 📖 参考：[ARM硬件笔试面试题型](https://zhuanlan.zhihu.com/p/1916057165761840418)

**🌙 晚练（项目叙事）**
- **关键数据**：FFT/均方根/滤波/插值 汇编优化 + FIR/IIR/FFT硬件加速器
- **核心叙事**：算法瓶颈在哪 → 汇编+硬加器方案 → 效果数据 → 可复用方法论

### Day 5：电力监控仪表 + DMA优化（爱博精电）
**🌅 晨学（ARM架构-下）**
- Cortex-M 中断向量表结构（前16个系统异常 + 外设中断）
- NVIC 中断优先级分组（抢占优先级 vs 子优先级）
- MPU（内存保护单元）vs MMU（内存管理单元）— 区别和应用场景
- HardFault 常见原因：非对齐访问/非法内存/未定义指令/除零
- 练习：手绘中断向量表 + 解释 HardFault 定位四步法
- 📖 参考：[Cortex-M面试核心考点](https://www.nowcoder.com/discuss/725288479193624576)

**🌙 晚练（项目叙事）**
- **关键数据**：DMA链式传输替代CPU轮询，CPU负载率 90%→47%，高实时性 20ms 周期
- **核心叙事**：CPU瓶颈在哪 → DMA链式方案设计 → 踩坑（DMA与Cache一致性）→ 实时性保障思维

### Day 6：低成本L2交换机（新华三）
**🌅 晨学（C语言底层-下）**
- 字节对齐原理 + `#pragma pack` 副作用 + 0x69a9→HardFault 九号真题
- 指针函数 vs 函数指针（声明区别 + 使用场景：回调/解耦/状态机）
- `sizeof` 数组在函数参数中的陷阱（退化为指针）
- 结构体内存布局（对齐规则、填充字节、`__attribute__((packed))`）
- 练习：写一个包含 char/int/short 的结构体，手工计算 sizeof
- 📖 参考：[嵌入式C语言面试常考题](https://www.nowcoder.com/discuss/841222926857666560)

**🌙 晚练（项目叙事）**
- **关键数据**：降本30%、RTL MAC+C51方案、WEB管理+云端纳管
- **核心叙事**：为什么选廉价方案 → 驱动→BOOT→量产全流程 → 降本30%怎么做到的 → 产品价值

### Day 7：PL管理经验（跨项目综合）
**🌅 晨学（RTOS-下）**
- 内存管理：静态内存池 vs 动态堆分配（确定性 vs 灵活性）
- FreeRTOS 五种堆方案 (Heap_1~5) 区别与选型
- 任务间通信全景：队列/信号量/互斥锁/事件组/任务通知（各自动用场景）
- 中断下半部机制（Tasklet/工作队列，嵌入式中的 Deferred Interrupt 处理）
- 练习：画任务通信全景图，标注每种机制的典型使用场景
- 📖 参考：[技术面试知识点总汇](../tech-interview-notes.md) 第一节

**🌙 晚练（项目叙事）**
- **关键数据**：5人团队、培养3骨干、IPD流程
- **核心叙事**：带团队的核心 → 建规范+拆任务+控风险 → 培养人的方法论

---

## 五、面试展示顺序（固化思朗反思）

无论面什么岗位，自我介绍和项目介绍的顺序固定为：

```
1. 核心能力（最对口的那一项，打面试官的第一印象）
   例：面MCU驱动岗 → 先讲 RTOS/底层驱动
   例：面TSN相关岗 → 先讲 TSN协议栈
   
2. 项目成果（数据说话，证明你能落地）
   
3. PL管理（加分项，证明你不是一个人干活的）
   
4. 工程体系（证明你在流程规范上的积累）
```

---

## 六、面后复盘模板

每次面试当天晚上，记录：

```markdown
## [公司名] 面试复盘 — [日期]

### 面试问到了哪些问题？
1. 
2. 

### 哪些问题回答得好？（叙事做到了→本质→实践→坑→思考）
-

### 哪些问题又回到"知识复述"了？
-

### 面试官追问了什么？（说明他关心什么）
-

### 下次改进
-
```

---

## 七、每日时间安排

| 时段 | 内容 | 时长 |
|------|------|:--:|
| **🌅 早上** | **基础知识系统学习**（ARM/C/RTOS/CPU轮转）+ 记录笔记 | 30分钟 |
| 🌤️ 通勤 | 回顾昨晚叙事稿，脑中过一遍关键数据 | 碎片时间 |
| **🌙 晚上** | **项目叙事训练**（写稿+录音+回听改进） | 30分钟 |
| 📅 周末 | 完整模拟面试，串讲本周项目+本周基础知识速问自测 | 1-2小时 |

### 晨学节奏建议
- **前15分钟**：阅读参考文章/复习昨日内容
- **后15分钟**：口头用自己的话讲一遍（录音）+ 手绘关键图表
- **不追求一次性全懂**：同一主题第二天轮转回来时加深，7天后每个模块已经过了1-2遍 |

---

## 八、端侧AI及博客计划（off-hold）

> ⚠️ **等到拿到offer后再启动**，原因见「当前核心矛盾」。

恢复后的优先级：
1. TFLM端到端推理流程（训练→量化→转换→部署）
2. 性能优化目标：推理≤10ms，内存≤100KB
3. GitHub开源 + README
4. 3篇技术博客（底层视角看端侧AI性能优化）

---

## 九、关键提醒

1. **不要等到"准备好"再投**——练完3个项目的叙事就可以开始投了
2. **每次面试 = 一次免费练习**——不要怕被拒，怕的是被拒后没有复盘
3. **把面试官当同事，不是考官**——叙事感比考试感强100倍
4. **数据是武器**——10KB/90%→47%/≤10ms/≤20ms/降本30% 这些数字张口就来
5. **思朗的教训**：先讲最值钱的，别从最基础的开始讲

---

**关联文档**：
- [[../company-interviews/ninebot-deep-review|九号一面深度复盘]]
- [[../company-interviews/ninebot-review|九号备战完整版]]
- [[./career-profile|完整职业画像 v1.0]]
