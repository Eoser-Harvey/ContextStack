# 技术知识点总汇

> 整合来源：RTOS笔记.docx + 嵌入式基础.docx/嵌入式基础2.docx + 嵌入式设计总结.docx + 九号牛客面经 + 九号备战文档 + gerrit-jenkins-ai-cicd.md
> 创建：2026-06-12

---

## 一、RTOS 核心（最高频考点）

### 1.1 自研 RTOS (AcuOS) 性能数据

| 指标 | AcuOS (M4) | AcuOS (DSP) | FreeRTOS (参考) |
|------|-----------|------------|--------------|
| 内核 ROM | ~5KB | ~10KB | ~6-10KB |
| 静态 RAM（零任务） | ~1.3KB | ~2.6KB | ~2-3KB |
| TCB 大小 | 24 bytes | 36 bytes | ~56 bytes |
| 每任务栈（默认） | 256B (64 words) | 800B (200 words) | 可配 |
| TCB 分配方式 | 静态数组预分配 768B | 动态 | 动态/静态可选 |
| 32任务额外 RAM | ~8KB（栈256B×32）| ~25.6KB（栈800B×32）| ≥17KB |
| IPC 支持 | Semaphore + Queue | Semaphore + Queue + Mutex + Flag + Alarm | 全量 IPC |
| 上下文切换 | PendSV（标准 Cortex-M） | DSP 汇编 | PendSV |

**已支持特性**：任务创建/优先级/栈配置(静态)、信号量、消息队列、软件定时器、互斥锁、系统时间、系统负载率

**新增特性**：标志服务(Flag)、闹钟(Alarm)

### 1.2 AMP 多核架构（SC573 实战）

```
A5(Linux) ── DSP1(RTOS) ── DSP2(裸机)
                │
        高速ADC+DMA采样 = 10ms
        5个应用任务 + 空闲任务：
          任务1: 10ms/20ms 高实时数据搬运+FFT
          任务2: 200ms 插值+谐波计算
          任务3: 3s 谐波
          任务4: 告警
          任务5: A5↔DSP双核通信(200ms周期)
        协处理器DSP2: 闪变/频率计算(裸机)
```

**同步机制分布**：
- `OsQPend` 队列：DI采集、EEPROM写入缓冲
- `OsFlagPend` 标志：按键处理、Modbus通信
- `T_OsMutex` 互斥锁：EEPROM写锁、IIC总线锁
- `T_OsSem` 信号量：EEPROM写、软件缓存

### 1.3 任务调度

**FreeRTOS 调度算法**：基于优先级的可抢占式调度 + 同优先级时间片轮转

**优先级抢占核心**：
- TCB 存储任务优先级
- 优先级位图 `uxTopReadyPriority` 快速定位最高就绪任务（O(1) 时间复杂度）
- 自研RTOS使用16-bit位图 + `__clz` 指令实现O(1)
- 触发切换时机：任务阻塞/中断退出/任务创建删除

**保证1ms级实时响应**：
- 核心实时任务设为高优先级
- 中断处理函数短小精悍（耗时操作通过信号量交后台任务）
- 减少不必要中断嵌套
- tick一般设为1ms，禁用不必要的钩子函数

**不可剥夺型 vs 可剥夺型内核**：
- 不可剥夺：任务必须主动放弃CPU → 几乎不需要信号量保护共享数据
- 可剥夺：高优先级任务可抢占 → 不能使用不可重入函数

### 1.4 优先级反转

**定义**：高优先级任务等待低优先级资源 → 低优先级被中优先级抢占 → 高优先级被无限期阻塞

**解决方案（3种）**：
1. **优先级继承**：低优先级持有锁时临时提升到等待者最高优先级，释放后恢复（FreeRTOS `xSemaphoreCreateMutex()` 默认支持）
2. **优先级天花板**：持有资源的任务提升到所有可能申请者的最高优先级
3. **编码规范规避**：缩短临界区/限制访问者/统一优先级/避免嵌套锁/中断下半部

**经典案例**：火星探路者号任务失败 ← 优先级反转未处理

**实战案例**（消防报警系统）：
- 高优先级报警任务等待低优先级存储任务释放Flash → 被中优先级传感器采集任务抢占
- 报警延迟最长达3ms（超出国标）
- 采用优先级继承后，延迟控制在0.5ms内

**链式反转问题**：多个任务各自持有不同资源互相等待 → 编码规范禁止锁内调用可能引发等待的函数

### 1.5 Tickless 低功耗机制

**五步流程**：
1. 空闲检测 → 2. 计算休眠时长 → 3. 配置LPTIM → 4. 进入休眠 → 5. 唤醒后时间补偿

**分数累加器算法**：32768/1000 非整数倍问题 → 累加余数补偿

**LPTIM vs RTC**：
- LPTIM：粒度30μs，分段休眠（16位定时器溢出需处理）
- RTC：粒度1s

**竞态条件防护**：关中断→读就绪表→决定休眠的窗口期必须原子化

### 1.6 内存管理

**4种 FreeRTOS 内存管理方式**：
- Heap_1：简单分配，不允许释放
- Heap_2：已淘汰
- Heap_3：包装标准 malloc/free（需线程安全）
- Heap_4：默认首选，支持释放合并
- Heap_5：支持不同非连续内存块

**静态 vs 动态**：RTOS 强烈推荐静态内存池 → 分配时间确定、无碎片、适合实时系统

**堆栈分配**：
- 使用 `uxTaskGetStackHighWaterMark()` 检测堆栈使用
- 典型需求：512B~4KB（根据局部变量和函数调用深度调整）

### 1.7 任务间通信

| 机制 | 用途 | 注意 |
|------|------|------|
| 队列 | FIFO数据传递 + 任务同步 | — |
| 信号量 | 资源计数、任务同步 | 二进制信号量有优先级反转风险 |
| 互斥锁 | 互斥访问 | **推荐**，内置优先级继承 |
| 事件组 | 多事件同步 | 任务可等待任意组合 |
| 任务通知 | 轻量级替代信号量 | FreeRTOS中比信号量快45% |

**中断设计原则**：ISR 快进快出，通过任务通知或队列传数据给任务

```c
// FreeRTOS中断发送通知
void IRQ_Handler() {
  BaseType_t xHigherPriorityTaskWoken = pdFALSE;
  vTaskNotifyGiveFromISR(xTaskHandle, &xHigherPriorityTaskWoken);
  portYIELD_FROM_ISR(xHigherPriorityTaskWoken);
}
```

### 1.8 Systick 和 PendSV 中断优先级

- Systick 和 PendSV 必须设为**最低优先级**
- 原因：避免在ISR中执行上下文切换，确保中断响应不被延迟
- PendSV用于实际切换，可被其他ISR抢占

---

## 二、C 语言底层（高频考点）

### 2.1 volatile

**三个使用场景**：
1. 硬件寄存器访问
2. ISR 与主程序共享变量
3. 内存映射 I/O

**三个常见误解**：
- ≠ 原子性
- ≠ 线程安全
- ≠ 内存屏障

### 2.2 const volatile

**使用场景**：只读硬件寄存器（值由硬件改变，软件不能写但每次必须从内存读取）

### 2.3 static 三种语义

1. **函数内**：持久局部变量（生命周期=程序运行期）
2. **文件内**：限制函数/变量作用域为本编译单元
3. **全局**：不可外部链接

### 2.4 函数指针 vs 指针函数

- **函数指针**：`int (*fp)(int, int)` — 指向函数的指针（回调/解耦/多态/状态机）
- **指针函数**：`int* func(int)` — 返回指针的函数

### 2.5 sizeof 陷阱

函数参数中的数组退化为指针 → `sizeof(arr)` 返回指针大小而非数组大小

### 2.6 字节对齐

- `#pragma pack` 改变对齐方式 → 可能导致非对齐访问
- ARM Cortex-M 外设总线：非对齐32-bit访问 → **HardFault**
- 自研RTOS中双核伪共享问题：两个变量同一Cache行 → 性能降40% → 64字节对齐解决

### 2.7 HardFault 排查（裸机四步法）

1. 保存栈帧 → 2. 提取PC（程序计数器）→ 3. CFSR 寄存器诊断（UsageFault/BusFault/MemManage）→ 4. .map 文件反查

### 2.8 GCC 优化等级

| 等级 | 特点 | 自研RTOS实践 |
|------|------|-------------|
| O0 | 无优化，调试用 | — |
| O1 | 基础优化（大小+速度平衡） | — |
| Os | 优化体积 | 内核用 Os，控制在 8KB |
| O2 | 标准优化 | 调度器用 O2 |
| O3 | 激进优化（内联+向量化） | 浮点运算用 O3，曾导致时序问题，用内存屏障解决 |

---

## 三、外设与协议

### 3.1 外设协议

| 协议 | 关键点 |
|------|--------|
| **I2C** | 开漏输出、上拉电阻、多主仲裁、时钟拉伸 |
| **SPI** | 全双工、4线/3线、时钟极性和相位(CPOL/CPHA) |
| **UART** | 异步、波特率、起始位/停止位/校验位 |
| **CAN** | 多主、仲裁(ID越小优先级越高)、错误帧、CAN FD对比（速率8Mbps、数据场64字节） |
| **CAN FD** | 仲裁段500kbps/数据段最高8Mbps → 兼容传统CAN |
| **SyncE** | 同步以太网时钟同步 |
| **Modbus** | 工业通信标准，RTU/ASCII/TCP三种模式 |
| **IEC 61850** | 电网设备通信标准 |

### 3.2 TSN 协议集

802.1Qbv：时间感知调度
802.1AS：时间同步
802.1Qci：流过滤和管控
802.1Qbu：帧抢占
802.1CB：无缝冗余
802.1Qcc：集中配置

**实战数据**：环网故障收敛时间 ≤20ms

### 3.3 DMA 优化案例

- **场景**：电力监控仪表，NXP K10 Cortex-M4
- **方法**：DMA链式传输替代CPU轮询
- **效果**：CPU负载率 **90%→47%**
- **坑**：DMA与Cache一致性问题，需在DMA前后做Cache Flush/Invalidate

---

## 四、中断与实时性

### 4.1 ISR 设计原则

- 快进快出
- 中断中只做必要的事（设标志/发信号量/发任务通知）
- 耗时操作交给任务处理
- 使用 `portYIELD_FROM_ISR()` 在ISR尾部触发任务切换

### 4.2 中断嵌套

- Cortex-M 支持可配置中断优先级
- NVIC 支持抢占（高优先级可打断低优先级ISR）
- 避免过深嵌套 → 增加栈使用和延迟不确定性

### 4.3 DMA vs 中断 vs 轮询

| 方式 | CPU占用 | 延迟 | 适用场景 |
|------|---------|------|---------|
| 轮询 | 高 | 低 | 简单、低频率场景 |
| 中断 | 中 | 低 | 中等数据量 |
| DMA | 低 | 取决于配置 | 大数据量、高吞吐 |

---

## 五、MCU 选型框架（12维度）

面试中展示技术选型能力的框架：
1. 处理器架构 → 2. Flash/RAM → 3. 主频 → 4. 外设 → 5. 功耗 → 6. 封装
7. 供货稳定性 → 8. 开发工具链 → 9. RTOS支持 → 10. 认证 → 11. 成本 → 12. 生态

---

## 六、CI/CD 与工程化

> 来源：gerrit-jenkins-ai-cicd.md

### 6.1 Gerrit 代码审查

- `refs/for/master`：提交到审查队列
- Change-Id：标识同一个逻辑变更的所有 Patch Set
- Patch Set：每次修改产生新版本

### 6.2 Jenkins Pipeline

- Jenkinsfile：Pipeline as Code
- Groovy DSL：定义流水线阶段
- Gerrit Trigger 插件：Review+2 → 自动合入

### 6.3 CI 五阶段流程

提交 → 验证（代码风格/静态分析/编译/单元测试/集成测试/覆盖率）→ 人工 Review → Submit

### 6.4 嵌入式 CI 特有挑战

- 交叉编译（arm-gcc）
- Docker 容器封装工具链
- HIL 硬件在环测试
- ROM/RAM 大小检查（CI 中自动对比 .map 文件）
- J-Link 烧录自动化

### 6.5 AI 融合 CI/CD（六注入点）

1. AI 代码风格检查（语义级，超越正则 Lint）
2. Commit Message 自动生成（Conventional Commits）
3. 自动化测试用例生成（根据 diff 生成 CppUTest 用例）
4. AI Code Review（内存安全/中断安全/并发安全/错误处理）
5. 失败根因分析（编译错误+测试日志 → AI诊断）
6. 架构合规检查（分层架构/禁用依赖/目录规范校验）

---

## 七、面试题目速查

### 7.1 FreeRTOS 精选题

| # | 题目 | 答案要点 |
|---|------|---------|
| 1 | 调度算法？ | 优先级抢占+时间片轮转，TCB+位图 O(1) |
| 2 | 优先级反转解决？ | 继承/天花板，火星探路者案例 |
| 3 | 堆栈分配？ | uxTaskGetStackHighWaterMark 检测，512B~4KB |
| 4 | 低功耗？ | Tickless：空闲→休眠→LPTIM唤醒→补偿 |
| 5 | 内存管理？ | Heap_4 默认首选，静态内存池推荐 |
| 6 | FreeRTOS vs 自研RTOS？ | 自研：10KB/40KB/0.4KB 更轻量，| 支持闹钟和标志服务 |
| 7 | 信号量 vs 互斥锁？ | 信号量=计数/同步 | 互斥锁=互斥+优先级继承 |
| 8 | 函数指针好处？ | 回调/解耦/多态/状态机/动态替换 |
| 9 | 链表应用？ | 就绪列表/延时列表/内存管理 |
| 10 | 共享内存保护？ | 关中断/互斥锁/消息队列代替 |

### 7.2 C语言精选题

| # | 题目 | 答案要点 |
|---|------|---------|
| 1 | volatile 使用场景？ | 寄存器/ISR变量/内存映射IO，≠原子性 |
| 2 | const volatile？ | 只读硬件寄存器 |
| 3 | static 三种语义？ | 函数内持久/文件内隐藏/全局不可外链 |
| 4 | sizeof 陷阱？ | 函数参数数组退化为指针 |
| 5 | 字节对齐 + HardFault？ | 非对齐32-bit访问ARM外设总线→HardFault |
| 6 | GCC 优化等级？ | O0/O1/Os(体积)/O2(标准)/O3 |

### 7.3 项目深挖题（高频）

| # | 题目 | 回答方向 |
|---|------|---------|
| 1 | 最难的技术问题？ | 按"本质→实践→坑→思考"四层讲述 |
| 2 | 系统架构怎么设计？ | 分层架构图 + 为什么这么分 |
| 3 | 如何保证产品质量？ | CI/CD + Review + 测试 + 流程 |
| 4 | 带团队最大挑战？ | 具体案例 + 解决方法 + 结果数据 |
| 5 | 如果从零设计XX产品？ | 需求→架构→选型→风险→计划 |

---

## 八、嵌入式 AI 速查（面试加分项）

### 8.1 部署全流程

模型训练(Python/TF) → 量化(FP32→INT8) → 格式转换(.h5→.tflite) → TFLM部署 → 推理

### 8.2 关键概念

- **INT8 量化**：将浮点模型转换为8位整数，减小体积、加速推理
- **TFLite Micro**：Google为MCU设计的轻量推理框架
- **算子注册**：TFLM使用算子注册表，可按需编译（减小体积）
- **Arena 内存**：TFLM使用预分配的Arena而非动态分配

### 8.3 端侧AI 定位话术

> "我不是算法科学家，我是算法与硬件之间的桥梁工程师。
> 能做模型部署、能做性能优化、能保证实时性和稳定性。"

---

**关联文档**：
- [[./narrative-training|MS叙事化训练计划]]
- [[./career-profile|完整职业画像 v1.0]]
- [[../company-interviews/ninebot-questions|九号-FreeRTOS面经14题]]
- [[../company-interviews/ninebot-review|九号备战完整版]]
- [[../embedded-fundamentals/|嵌入式基础知识(docx原始文件)]]
