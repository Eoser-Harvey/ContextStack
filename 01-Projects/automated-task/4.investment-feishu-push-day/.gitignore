# 飞书 Bot 凭证（本地保密，禁止提交）
.secrets.yaml

# Token 缓存（本地生成，无需提交）
.lark_token_cache.json

# Python
__pycache__/
*.py[cod]

# 运行时数据缓存（本地生成，无需提交）
data_cache/

# 代理运行时文件（mihomo 二进制 + 临时配置）
proxy/mihomo.exe
proxy/clash_runtime.yaml
