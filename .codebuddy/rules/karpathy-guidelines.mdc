---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-05-13T12:21:52.806Z
provider: 
---

# Karpathy 编码准则

基于 Andrej Karpathy 的 LLM 编码陷阱观察，约束 AI 编码行为。

## 四大核心原则

### 1. 先思考再编码（Think Before You Code）
- 收到需求后，先输出对需求的理解和实现思路，再动手编码
- 需求模糊时，显式列出假设并要求用户确认，禁止凭猜测实现
- 复杂任务先拆解为子任务，确认拆解方案后再逐个执行

### 2. 简洁优先（YAGNI - You Aren't Gonna Need It）
- 只实现当前明确需要的功能，不加推测性功能
- 不做过度抽象：3个以上相似场景才抽象，否则直接写
- 不加不必要的注释、日志、配置项
- 能用10行解决的，不用50行

### 3. 手术式修改（Surgical Changes）
- 只改必须改的，不顺手重构周边代码
- 每次修改的 diff 应该尽可能小
- 不改变无关的命名、格式、结构
- 如果发现需要大规模重构，先告知用户并获得确认

### 4. 目标驱动执行（Goal-Driven Execution）
- 每一行变更必须能追溯到用户请求
- 修改完成后，说明每处修改与需求的对应关系
- 不做"顺便"的优化或清理，除非用户明确要求

## 反模式警告

| 反模式 | 正确做法 |
|--------|----------|
| 拿到需求直接写代码 | 先输出理解和方案 |
| 一次改多个不相关的地方 | 分次、分区域修改 |
| "顺便"重构/优化 | 只做用户要求的 |
| 添加"以防万一"的功能 | 只做当前需要的 |
| 遇到模糊需求自行决定 | 显式列出假设，请求确认 |