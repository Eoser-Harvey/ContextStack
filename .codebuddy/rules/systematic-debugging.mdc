---
description: 
alwaysApply: false
enabled: true
updatedAt: 2026-05-13T12:22:34.893Z
provider: 
---

# Systematic Debugging Skill — 四阶段系统化调试

## 核心纪律
禁止猜测式修复。每个修复动作必须有明确的根因分析支撑。

## 执行流程

### Phase 1: 根因调查（Root Cause Analysis）
1. **收集信息**：完整读取错误信息、堆栈跟踪、相关代码
2. **复现问题**：确认问题的稳定复现条件
3. **定位边界**：确定问题在哪个模块/函数/文件中
4. **排除法**：逐一排除无关因素

### Phase 2: 模式分析
1. **检查同类问题**：搜索项目中是否有类似的错误模式
2. **检查最近变更**：问题出现前最近改了什么
3. **检查边界条件**：输入是否在预期范围内
4. **诊断日志**：在关键边界添加日志，收集运行时信息

### Phase 3: 假设测试
1. **提出假设**：基于Phase 1-2的分析，提出最可能的根因假设
2. **设计验证**：设计最小化的验证实验（改最少的代码来验证假设）
3. **验证假设**：执行验证，确认或推翻假设
4. **如果推翻**：回到Phase 1，不急于尝试下一个假设

### Phase 4: 实施修复
1. **最小化修复**：只改必须改的代码
2. **验证修复**：确认原问题不再出现
3. **回归测试**：确认修复没有引入新问题
4. **根因记录**：在任务记录中写入根因分析和修复方案

## 压力升级机制（来自PUA Skill）
- 同一问题修3次仍失败 → 触发架构质疑
  - 是否根本方案就有问题？
  - 是否需要换一个完全不同的思路？
  - 是否需要用户介入决策？
- 修5次仍失败 → 强制暂停，输出完整诊断报告请求用户指导

## 反模式
| 反模式 | 正确做法 |
|--------|----------|
| "可能是X问题，我改一下" | 先确认根因再改 |
| 同时改多处来"试试" | 每次只改一处验证 |
| 反复猜测式修复 | 3次失败后质疑思路 |
| 只修表面症状 | 找到根因并修复 |